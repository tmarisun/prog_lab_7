-- =============================================================================
-- Выполняйте ПОСЛЕ замеров без индексов (скриншоты уже сделаны).
-- Дополнительные индексы к уже существующим PRIMARY KEY / UNIQUE.
-- =============================================================================

-- Ускоряет фильтрацию и сортировку по числу жителей
CREATE INDEX IF NOT EXISTS idx_cities_population ON cities (population);

-- Ускоряет поиск по имени (точное совпадение =, префиксы с varchar_pattern_ops — опционально)
CREATE INDEX IF NOT EXISTS idx_cities_name ON cities (name);

ANALYZE cities;
