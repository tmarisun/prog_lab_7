org.gradle.console=plain

# Helios: меньше памяти у JVM демона; иначе «A problem occurred starting process Gradle build daemon».
org.gradle.daemon=false
org.gradle.jvmargs=-Xmx96m -XX:MaxMetaspaceSize=48m -XX:ReservedCodeCacheSize=24m -XX:InitialCodeCacheSize=8m -XX:+UseSerialGC
org.gradle.workers.max=1

# --- Helios / удалённый демонстрационный стенд (опционально) ---
# heliosHost=helios.cs.ifmo.ru
# serverPort=5555
# pgHost=pg
# pgDatabase=studs
# pgUser=
# autoCreateDatabase=true
